/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    images: {
        domains: ['www.m4mformen.com'],
        unoptimized: true
    }
}

module.exports = nextConfig
